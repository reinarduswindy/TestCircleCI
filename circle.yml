machine:
  environment:
    TKPENV: "circleci"
    GODIST: "go1.7.4.linux-amd64.tar.gz"
  services:
    - redis
    - elasticsearch

# dependencies:
#   override:
#     - if [ $CIRCLE_BRANCH = 'master']; then go test -v ./...; fi;

general:
  branches:
    ignore: staging
